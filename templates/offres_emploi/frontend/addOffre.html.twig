{% extends 'base.html.twig' %}

{% block body %}
<div class="form-container-centered">
    <a href="{{ path('showOffre') }}" class="back-link">
        <i class="fas fa-arrow-left"></i> Retour à la liste
    </a>

    <h1 class="edit-title">
        <i class="fas fa-plus-circle text-primary"></i> Publier une nouvelle offre
    </h1>

    <div class="formation-form-wrapper">
        {{ form_start(formOffre) }}
            <div class="row">
                {# On garde la même structure de grille que tu as déjà #}
                <div class="col-md-12 form-group-custom">
                    {{ form_label(formOffre.nom_entreprise, 'Nom de l\'Entreprise') }}
                    {{ form_widget(formOffre.nom_entreprise, {'attr': {'placeholder': 'Ex: Intellink Tech'}}) }}
                </div>
                
                <div class="col-md-12 form-group-custom">
                    {{ form_label(formOffre.description, 'Description du poste') }}
                    {{ form_widget(formOffre.description, {'attr': {'placeholder': 'Décrivez les missions et prérequis...'}}) }}
                </div>

                <div class="col-md-6 form-group-custom">
                    {{ form_label(formOffre.type_contrat, 'Type de Contrat') }}
                    {{ form_widget(formOffre.type_contrat) }}
                </div>

                <div class="col-md-6 form-group-custom">
                    {{ form_label(formOffre.statut, 'Statut initial') }}
                    {{ form_widget(formOffre.statut) }}
                </div>

                <div class="col-md-12 form-group-custom">
                    {{ form_label(formOffre.salaire, 'Salaire (€)') }}
                    {{ form_widget(formOffre.salaire) }}
                </div>

                <div class="col-md-6 form-group-custom">
                    {{ form_label(formOffre.date_debut, 'Date de début') }}
                    {{ form_widget(formOffre.date_debut) }}
                </div>

                <div class="col-md-6 form-group-custom">
                    {{ form_label(formOffre.date_expiration, 'Date d\'expiration') }}
                    {{ form_widget(formOffre.date_expiration) }}
                </div>
            </div>

            <div class="form-actions">
                <button type="submit" class="btn-success-custom">
                    <i class="fas fa-paper-plane"></i> Publier l'offre
                </button>
                <a href="{{ path('showOffre') }}" class="btn-secondary-custom">Annuler</a>
            </div>
        {{ form_end(formOffre) }}
    </div>
</div>
{% endblock %}